import React from 'react';
import ReactDOM from 'react-dom/client';
import CoiAutopilot from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CoiAutopilot />
  </React.StrictMode>
);
